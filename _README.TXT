o обратите внимание, что скрипту требуется установленный пакет openxlsx для чтения и записи экселевских файлов
  для этого надо набрать в R следующую команду:
install.packages(c('openxlsx'), dependencies = c('depends','imports'))